{
  "name": "consensus-ai-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:token:sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:factory:sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:all:sepolia": "hardhat run scripts/deploy.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}
